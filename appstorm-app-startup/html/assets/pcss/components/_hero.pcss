/*-------------------------------------------
  -- Hero
-------------------------------------------*/
#ts-hero {
  color: var(--color-white);
  display: flex;
  position: relative;

  .ts-background-image {
    background-position: top center;
  }
}